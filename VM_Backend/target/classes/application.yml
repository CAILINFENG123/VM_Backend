# mysql
spring:
  datasource:
    #MySQL����
    driverClassName:  com.mysql.cj.jdbc.Driver
#    project is where you put the database
    url: jdbc:mysql://localhost:3306/?projectuseUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 1234

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.model
server:
  port: 3000
